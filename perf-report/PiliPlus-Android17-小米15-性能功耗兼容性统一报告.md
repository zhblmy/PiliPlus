# PiliPlus Android 体验优化报告（Android 17 · 小米 15 · 澎湃 OS 4）

> **本报告只回答两件事**：① **是否已实施**；② 实施后**用户能感知到什么**。
> 背景论证、原始问题清单、代码走查细节不再保留（需要时见 git 历史与两份来源报告 `PiliPlus-Android.md`、`PiliPlus-Android17-Xiaomi15.md`）。
>
> **落点纪律**：只做影响 Android 的改动 —— 能只改 `android/` 就只改 `android/`；必须改 `lib/` 时用 `Platform.isAndroid` / `DeviceUtils.sdkInt` 守卫，不改 iOS 与桌面行为。
>
> **最后更新**：2026-09-26（完成「零风险快赢」与「可见性门控」两批）

---

## 一、批次总览

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| 0 | Android 17 适配（后台音频 / 本地网络 / 内存预算 / 预测性返回） | ✅ 已完成 |
| 1 | 零风险快赢（清单、配置、统计与缓存） | ✅ 2026-09-26 完成 |
| 2 | 应用可见性统一门控（熄屏/后台耗电主线） | ✅ 2026-09-26 完成骨架 + 3 处接入 |
| 3 | 卡顿与发热主线（直播弹幕 isolate、评论缓存、每帧重建） | ⏳ 未开始 |
| 4 | 后台长任务与更新链路（下载前台服务、应用内安装、正式签名） | ⏳ 未开始 |
| 5 | 画质与渲染后端（HDR、Impeller A/B、解码线程） | ⏳ 未开始 |
| 6 | 发布（R8、16 KB 页对齐、大屏方向） | ⏳ 未开始 |

---

## 二、已实施

### 2.1 Android 17 适配（批次 0）

| 编号 | 内容 | 用户可感知效果 |
| --- | --- | --- |
| A17-01 | 后台音频加固：前台服务先于音频写入进入前台（WIU 能力） | 后台/熄屏播放不再被系统**静默掐断**（以前会「莫名停了」且无报错） |
| A17-02 | 声明并申请 `ACCESS_LOCAL_NETWORK` | DLNA 投屏在 Android 17 上能搜到设备；被拒绝时提示「去设置」而不是「没有设备」 |
| A17-03 | 内存预算：图片缓存与播放缓冲按设备内存分级 | 低内存机不被系统杀后台；大内存机图片缓存更足、滚动更顺 |
| A17-06 | `enableOnBackInvokedCallback` 改 `true` | 侧滑返回与系统手势一致，不再出现返回动画卡住/重复返回 |
| A17-12 | 蓝牙重配对、跨配置回环、Keystore 上限 | 无感知（确认无影响） |

### 2.2 澎湃 OS / 小米 15（批次 0）

| 编号 | 内容 | 用户可感知效果 |
| --- | --- | --- |
| MI-02 | 场景化刷新率（视频 ≤45fps 降到 60Hz 档） | 看番更省电、机身更凉；滑动时仍回高刷 |
| MI-03 | 通知权限请求 + 澎湃媒体控件 | 首次进入会申请通知权限；锁屏/通知栏出现澎湃版媒体卡片 |
| MI-04 / MI-09 | 省电模式 / 低电量 / 温控降档（降刷新率、关超分、缩弹幕、降缓冲） | 电量低或机身热时自动降档并**弹一次提示**，不再一边发烫一边掉电 |
| MI-08 | 音频输出后端顺序 aaudio→audiotrack→opensles | 蓝牙/有线切换时爆音、无声的概率下降 |
| MI-13 / MI-14 | 澎湃 OS 兼容性检查页（通知/电池优化/自启动/后台弹出界面一键跳转）+ 刷新率被系统覆盖时告警 | 解决「后台被杀、收不到消息」；能看出刷新率设置是否真的生效 |
| MI-15 | 持续性能模式 | 长时间播放 + 弹幕时帧率更平缓，不再先冲高频再骤降 |
| MI-16 | 内存压力主动释放 | 系统内存紧张时先清图片缓存 + 降缓冲，而不是直接被系统杀掉 |

### 2.3 播放与解码（批次 0）

| 编号 | 内容 | 用户可感知效果 |
| --- | --- | --- |
| PL-01 | Impeller 开关化（默认关） | 可 A/B 实测；默认保证兼容性 |
| PL-02 | `vo` / `gpu-api` 可配置 | 个别机型花屏时可切 Vulkan 后端自救 |
| PL-04 | AV1 硬解 | AV1 片源走硬解，同画质更省电 |
| PL-05 | 硬解失败自动降级链 | 个别片源黑屏/花屏时自动换解码方式，不用手动改设置 |
| PL-06 | Android `video-sync` 默认改 `audio` | LTPO 高刷机型播放更省电，音画同步更稳 |
| PL-07 | 直播重连退避（3/6/12/24s） | 网络抖动时不再无限重试，恢复后自动重连 |
| PL-08 | 画中画降载（关超分、恢复不熄屏） | 小窗边看边刷更省电 |

### 2.4 启动与唤醒（批次 0）

| 编号 | 内容 | 用户可感知效果 |
| --- | --- | --- |
| S-01 ~ S-04 | 冷启动重排（音频服务去 await、MediaKit 延后到首帧后、动态取色并行、Hive box 冷热分离） | **冷启动更快**，点图标到能看见首页的时间明显缩短 |
| 阶段一 | 唤醒锁按「前台 + 播放中 + 可见/PiP」门控、心跳 5s→15s | 熄屏后 CPU/网络不再全速工作：待机掉电和发热下降 |
| 阶段一 | 骨架屏全局单 Ticker | 列表加载中的 shimmer 更省电 |
| 阶段一 | 日志批量 flush + 轮转、默认 `maxCacheSize` 256 MiB | 缓存不会无限膨胀；异常日志不再频繁写盘 |

### 2.5 UI / 交互（2026-09-25 批次）

| 内容 | 用户可感知效果 |
| --- | --- |
| 页面过渡默认改 X 轴推进（`sharedAxis`） | 转场**结束那一帧不再「卡一下」** |
| 转场收尾减压（`runAfterRouteAnimation`） | 返回首页/视频页时不再与网络请求、播放器恢复抢帧 |
| 首页搜索框缩小 + 行高固定 | 搜索栏收起/展开不再偏移；登录前后行高不再跳 13px |
| 动态页 Tab 与首页统一（42 高、下划线位置一致） | 两页观感一致 |
| 液体玻璃顶栏/底栏 | 视觉更现代、更像新系统（成本治理见 U-18） |
| 图标与动态取色修复 | Android 12+ 图标恢复品牌绿并能跟随壁纸；暗色下页面层次不再糊成一片 |
| 徽章/标题字重治理 | 「已关注」等徽章不再一律超粗，列表层次更清楚 |
| 构建：javac 中文提示乱码修复 | 构建日志可读（开发者体验） |

### 2.6 2026-09-26 本次实施

| 编号 | 内容 | 用户可感知效果 |
| --- | --- | --- |
| MI-06 | 夜间启动图 `#212121` → `#000000` | 暗色模式下冷启动第一下不再先闪一块灰；AMOLED 上更省电、观感更整 |
| BD-02 | 关闭 `android.enableJetifier`（已核对 release 运行依赖中无 `com.android.support` / `android.arch.*`） | 构建更快（开发者体验）；运行行为无变化 |
| I-01b | 缓存体积统计移到独立 isolate 执行（口径不变） | 「设置 → 关于 → 缓存大小」进入时不再掉帧/卡顿 |
| U-08 | 相对时间结果按分钟缓存 | 长列表（动态/评论/私信）滚动更顺；时间文案不会过期 |
| PF-03 | **应用可见性统一门控**（新 `lib/services/app_visibility.dart`） | 本身无感；它是下面三张卡以及后续「熄屏即停」的统一挂载点 |
| L-02 | 直播消息长连接「应用不可见即断开、回前台重连」 | 熄屏/切后台后直播不再偷偷收消息 + 每 30s 心跳：**待机掉电与流量下降**，回前台自动重连 |
| B-07 | 定时关闭的 1 秒倒计时不可见即暂停 | 熄屏后不再每秒唤醒一次（剩余时间由绝对 deadline 重算，关闭时间不走偏） |
| B-06 | 登录二维码 1 秒轮询不可见即暂停 | 后台不再每秒请求扫码状态；回前台若仍在二维码页会自动重新获取 |
| A17-04 | 新增 `network_security_config.xml` | 无感知（预防性）：把 CT/ECH/明文策略显式化，便于排障与演进 |
| A17-05 | 新增 FileProvider + 显式 `FLAG_GRANT_READ_URI_PERMISSION` | 无感知（预防性）：避免 Android 18 上「分享/安装包打不开」，也是应用内更新的前置 |
| 加固 | 直播建流去重 + 断开时作废飞行中的 token 请求；二维码刷新并发保护 | 短时间内反复切前后台时，不会重复建直播长连接、不会多发一次二维码请求（不会偷跑流量/耗电） |

---

## 三、未实施（按建议顺序）

### 3.1 批次 3：卡顿与发热（体感最强）

| 编号 | 内容 | 实施后的用户可感知效果 |
| --- | --- | --- |
| L-01 | 直播弹幕/礼物/SC 解析移出 UI isolate | 热门直播间不再因弹幕刷屏掉帧、发热 |
| U-02 | 评论正则 + `TextSpan` 结果缓存 | 长评论列表滚动帧率明显提升（**最体感的一条**） |
| U-03 / U-04 / U-16 | 评论增删改改为局部更新、拆分 `Obx` | 删评论/置顶不再整列表重建（不再闪一下） |
| U-05 | 顶栏/底栏滚动 offset 改 `ValueListenableBuilder` | 滚动时每帧重建次数下降，滑动更跟手 |
| U-17 | `ThemeData` memo 化 | 切主题/深浅色切换不再卡顿 |
| U-06 / U-09 / U-10 / L-05 | 去 `Opacity`、去多余 `saveLayer`、统一缩略图 | 列表滚动更省 GPU、图片加载更快 |
| U-14 / U-15 | 补 `cacheExtent` / `prototypeItem` / 懒加载 | 快速滑动白屏变少；搜索建议不再卡 |
| U-18 | 液体玻璃 `BackdropFilter` 成本治理 + 接低功耗自动降档 | 新玻璃 UI 不再拖累续航与滚动帧率 |
| D-02 / D-06 | 弹幕 Canvas 不可见暂停、`TextPainter` 缓存 | 后台不再白画弹幕；弹幕密集时 CPU/发热下降 |
| P-08 / P-09 | 视频页每帧级重建收口（`_animListener`、悬浮工具栏） | 播放器展开/收起动画与悬浮工具栏不再掉帧 |
| L-03 / L-04 | SuperChat 定时器合并、直播间 5 分钟定时器门控 | 直播间后台不再反复唤醒 |

### 3.2 批次 2 续：熄屏/后台耗电

| 编号 | 内容 | 实施后的用户可感知效果 |
| --- | --- | --- |
| B-09 | 其余定时器统一挂到 PF-03 门控 | 待机 8 小时掉电继续下降 |
| B-02 | 回前台/切 Tab 的请求合并去重 | 切回 App 时列表不再「闪一下再刷新」 |
| B-04 | 连接池 `idleTimeout` 15s → 60~120s | 频繁切换时少一次 TLS 握手，弱网更明显 |
| B-03 / B-06 / B-07 | （已完成 paused/退页部分，剩余边缘场景） | 已覆盖主要路径 |
| B-08b | 通知渠道细分（播放/下载/更新分离） | 可以只静音「下载完成」而保留「正在播放」 |
| L-02 / D-02 | （已接入门控） | 见批次 2 |

### 3.3 批次 5：网络与序列化

| 编号 | 内容 | 实施后的用户可感知效果 |
| --- | --- | --- |
| N-01 | gRPC 解析开 `isolate: true`（36 个调用点当前只有 1 处开） | 打开动态/评论/私信列表时不再卡一下 |
| N-02 | `responseDecoder` 异步化（解压离开主 isolate） | 大响应（评论、动态）下载后解压不再阻塞 UI |
| N-03 | 主 isolate 逐条 `jsonDecode` 批量化 | 同上 |
| N-05 | 连接复用与网络切换策略 | 地铁/电梯切网后不再请求失败重试 |
| N-04 | HTTP/2 评估（只测不默认开） | 待定 |

### 3.4 批次 4：后台长任务与更新链路

| 编号 | 内容 | 实施后的用户可感知效果 |
| --- | --- | --- |
| MI-11 | 下载/导出改前台服务（`dataSync`）+ 进度通知 | 澎湃 OS 上后台下载不再被冻结中断，且能看到进度 |
| MI-12 | 应用内更新安装链路（下载 APK + FileProvider 安装） | 有新版时在 App 内直接下载安装，不用跳浏览器 |
| BD-04 | 正式签名（`key.properties` 目前不存在 → release 用 debug 签名） | **最影响升级体验的一条**：签名不同无法覆盖安装，用户必须卸载重装、丢数据 |
| BD-01 | release 开 R8 + 资源压缩 | 安装包更小、启动更快 |
| BD-06 | 符号表留存与体积核算 | 无感知（崩溃定位） |

### 3.5 批次 5：画质与渲染后端

| 编号 | 内容 | 实施后的用户可感知效果 |
| --- | --- | --- |
| PL-03 | HDR10 / 杜比视界输出与 tone-mapping | 小米 15 上看 HDR 片源亮度与对比明显提升 |
| PL-04b | `hwdec-codecs` 白名单 | 个别编码不再错误走软解（省电） |
| PL-09 | 截图/导出（WebP）路径的 mpv 实例生命周期 | 连续导出图片/WebP 不再 OOM |
| PL-10 | 播放器实例复用复核 | 视频页返回再进不再重新初始化（更快） |
| PL-11 | 解码线程数与功耗曲线 | 高码率片源发热下降 |
| PL-12 | 移动网络不预取 + 弱网自动降清晰度 | 流量更省；弱网自动降档不卡 |
| MI-17 | 持续性能模式 × 澎湃性能模式实测收口 | 可能改为「仅插电时开启」，避免电池模式下白费电 |

### 3.6 批次 5/6：启动、发布与体验增强

| 编号 | 内容 | 实施后的用户可感知效果 |
| --- | --- | --- |
| S-05 / S-11 | 启动期请求合并、原生调用时序重排 | 冷启动再快一点；首帧更早 |
| S-06 / S-07 / S-09 / S-10 | GetX 生命周期治理、路由懒构造、切 Tab 防抖 | 内存占用更低、切 Tab 不再抖 |
| I-03 / I-04 | `Pref` 读时写盘、字体/JNI 延后到首帧 | 冷启动更快 |
| MI-07 | 16 KB 页对齐校验 | 无感知（发布前置，新设备兼容） |
| A17-07 | 字体反射兜底 + `.so` 只读校验 | 个别 ROM 上「设置字体」不再闪退/白屏 |
| A17-09 | 大屏（sw≥600dp）方向策略 | 折叠屏/平板横屏不再被强制竖屏（本机无感） |
| A17-10 | 旋转后 IME 可见性回归 | 旋转后键盘与焦点不丢 |
| A17-11 | `FLAG_SECURE`（可选功能） | 登录页/敏感页防截屏与最近任务预览 |
| A17-13 | 部分照片访问 + 选图权限降级 | Android 14+ 选图页能选「更多照片」，不再只能选一次 |
| MI-01 | 自启动白名单 + 电池优化引导（与 MI-13 同源） | 后台播放/下载不再被系统清掉 |
| FT-01 | 动态快捷方式 | 长按图标直达「稍后再看/历史/搜索」 |
| FT-03 | 主题图标 monochrome 验证 | 桌面图标跟随澎湃 OS 主题 |
| FT-04 | PiP 增强（内部弹幕开关、无缝 resize） | 多任务体验更接近原生（播放控制已实现） |
| FT-06 | `localeConfig` + 应用内语言切换 | Android 13+「按应用语言」设置生效 |
| FT-08 | 无障碍专项（`contentDescription`、Android 17 复杂 IME） | TalkBack 可用；中文输入法候选栏不再错位 |

### 3.7 报告外新增建议

| 建议 | 实施后的用户可感知效果 |
| --- | --- |
| 液体玻璃模糊接 `DeviceState.isLowPower` / 温控自动降档 | 省电或发热时玻璃自动变「素」，续航不被新 UI 吃掉（属 U-18 落地方式） |
| 深色模式增加「定时 / 跟随日出日落」 | 现在只有 亮/暗/跟随系统，没法定时切换 |
| 崩溃/异常上报开关（当前 `enableLog` 默认关 → release 无上报） | 线上问题可视，是持续改进体验的闭环（需产品与隐私决策） |

---

## 四、明确不做 / 无需改

- `lib/common/widgets/flutter/**`：Flutter fork，改动风险高，不动。
- 滚动过度效果：`useMaterial3: true` 已走系统拉伸回弹，无需改（仅 video/member/dynamics_detail 有意关闭）。
- `PBadge` 默认字重、M3 motion tokens、动态取色 `variant` 硬编码、`isPureBlackTheme` 默认 false：现状自洽，不动。
- 米 UI 对第三方 App 的限制（如「按应用自定义刷新率」）：已用 MI-14 检测并提示，不试图绕过。

---

## 五、验证口径

- 每张卡：`dart analyze lib`（基线 **36 info / 0 error**）→ `flutter build apk --release --target-platform android-arm64 --split-per-abi --dart-define-from-file=pili_release.json --no-pub` → 真机回归 → 记录验收项。
- 本机全量 Gradle 构建约 2~4 min（`kotlin.incremental=false`，见 BD-02 说明），不要误判为卡死；构建期间不要并发跑其它 `gradlew` 命令。
- 回归设备：小米 15（澎湃 OS 4，主）+ 一台 Android 12/13 旧机（验证 `Platform.isAndroid` / `sdkInt` 守卫没写反）。
- 关键量化场景（改动前后各测一次）：待机 8 h 电流与唤醒次数；冷启动 `am start -W` 的 `TotalTime`；1080P60 播放 30 min 的 CPU/电量；视频页滚动 3 min 的 P95 帧时间；热门直播间 10 min 的 UI isolate CPU。
